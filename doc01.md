# SDU-WebDav プロジェクトのソースコード解説

このプロジェクトのファイル (`SDU_webDav.h`, `main.cpp`, `SDUpdater.cpp`, `fs_util.cpp`) は、ESP32（特にM5Stackデバイス）上で動作するWebDAVサーバープロジェクト「SDU-WebDav」のソースコードです。ユーザーはWiFi経由でデバイスのファイルシステム（SDカードまたはSPIFFS）にアクセスできます。

## 1. `SDU_webDav.h` (ヘッダーファイル)

*   **役割:** プロジェクト全体で共通して使用される設定、ライブラリのインクルード、関数や変数の宣言を行います。
*   **主な内容:**
    *   **インクルードガード (`#ifndef`, `#define`, `#endif`):** ヘッダーファイルが複数回読み込まれるのを防ぎます。
    *   **ライブラリのインクルード:**
        *   `Arduino.h`, `M5Unified.h`: ArduinoフレームワークとM5Stackデバイス用の基本ライブラリ。
        *   `WiFi.h`, `ESPmDNS.h`: WiFi接続とmDNS（ホスト名でのアクセス）機能用ライブラリ。
        *   `SD.h`, `SPIFFS.h`, `LITTLEFS.h`: SDカード、SPIFFS、LITTLEFSといったファイルシステムを扱うためのライブラリ。
        *   `time.h`: 時刻関連機能用ライブラリ。
        *   `ESPWebDAV.h`: WebDAVサーバー機能を提供するコアライブラリ。
    *   **外部関数宣言 (`extern`):** 他のファイル (`fs_util.cpp`) で定義されている関数 (`setupNetwork`, `STOP`, `prt`) を、このヘッダーをインクルードするファイルから呼び出せるように宣言しています。
    *   **外部変数宣言 (`extern`):** プロジェクト全体で共有されるグローバル変数 (`PROG_NAME`, `VERSION`, `GITHUB_URL`, WiFi設定関連の変数など) を宣言しています。これにより、各 `.cpp` ファイルからこれらの変数にアクセスできます。

## 2. `main.cpp` (メインプログラムファイル)

*   **役割:** プログラムの実行開始点 (`setup` 関数) とメインループ (`loop` 関数) を含みます。全体の初期化、WebDAVサーバーの起動、クライアントからのリクエスト処理を行います。
*   **主な内容:**
    *   **インクルード:** `SDU_webDav.h` (共通定義) と `SDUpdater.h` (SDカードからのアップデート機能) をインクルードします。
    *   **定数定義:** プログラムのバージョン (`VERSION`) とGitHubリポジトリのURL (`GITHUB_URL`) を定義します。
    *   **ファイルシステム選択 (`#if defined(ENABLE_SD_SELECT)`):**
        *   コンパイル時のオプション (`ENABLE_SD_SELECT`) によって、WebDAVサーバーが使用するファイルシステムをSDカード (`SD`) または SPIFFS (`SPIFFS`) に切り替えます。
        *   `PROG_NAME` (プログラム名) も選択されたファイルシステムに応じて設定されます。
    *   **設定項目 (`SETTINGS`):**
        *   `WIFI_TXT`: WiFi設定 (SSID, パスワード, ホスト名) を記述するファイル名を定義します。このファイルがSDカードやSPIFFSにあれば、その設定が優先されます。
        *   `YOUR_SSID`, `YOUR_SSID_PASS`, `YOUR_HOST_NAME`: `wifi.txt` が見つからない場合のデフォルトのWiFi設定とホスト名を定義します。
        *   `DISP_ON`: M5Stackのディスプレイにメッセージを表示するかどうかのフラグです。
    *   **グローバルオブジェクト:**
        *   `WiFiServer tcp(80)`: WebDAVが使用するTCPサーバーをポート80で初期化します。
        *   `ESPWebDAV dav`: WebDAVサーバーのインスタンスを作成します。
    *   **`setup()` 関数:**
        *   M5Stackデバイスの初期化 (`M5.begin()`) を行います。
        *   `ENABLE_SD_UPDATER` が有効な場合、SDカードからのファームウェア更新機能 (`SDU_lobby()`) を呼び出します。
        *   ディスプレイの明るさや文字サイズを設定します。
        *   `fs_util.cpp` 内の `setupNetwork()` 関数を呼び出して、ファイルシステムの初期化、WiFi設定の読み込み、WiFi接続、mDNSの開始などを行います。失敗した場合は `STOP()` 関数で処理を停止します。
        *   TCPサーバー (`tcp.begin()`) とWebDAVサーバー (`dav.begin()`) を起動します。WebDAVサーバーには、使用するファイルシステム (`DAV_FS`) を渡します。
        *   接続に使用するネットワークパス (`\\HOSTNAME\DavWWWRoot` の形式) をシリアルモニタとディスプレイに出力します。
    *   **`loop()` 関数:**
        *   `dav.handleClient()` を繰り返し呼び出し、WebDAVクライアントからの接続要求やファイル操作リクエストを処理します。
        *   `delay(1)` で短い待ち時間を入れ、他のタスクにCPU時間を譲ります。

## 3. `SDUpdater.cpp` (SDカードアップデーター関連ファイル)

*   **役割:** M5StackUpdaterライブラリを利用して、SDカードに置かれたファームウェア (`.bin` ファイル) からプログラムを更新する機能を提供します。`ENABLE_SD_UPDATER` マクロが定義されている場合のみコンパイル・使用されます。
*   **主な内容:**
    *   **条件コンパイル (`#if defined(ENABLE_SD_UPDATER)`):** このファイルの内容は `ENABLE_SD_UPDATER` が定義されている場合にのみ有効になります。
    *   **インクルード:** `Arduino.h`, `M5Unified.h`, `M5StackUpdater.h`, `SDUpdater.h` をインクルードします。
    *   **定数定義:** `SDU_SKIP_TMR` で、アップデーター画面を自動的にスキップするまでの時間を定義します。
    *   **`SDU_lobby()` 関数:**
        *   `main.cpp` の `setup()` 関数から呼び出されます。
        *   `SDUCfg` を使って、アップデーター画面に表示されるアプリケーション名やメニューボタンのラベルを設定します。
        *   `checkSDUpdater()` 関数を呼び出して、SDカード上の指定されたバイナリファイル (`MENU_BIN`) をチェックし、必要に応じて更新プロセスを開始します。

## 4. `fs_util.cpp` (ファイルシステム・ネットワークユーティリティファイル)

*   **役割:** ファイルシステム (SDカード, SPIFFS) の初期化、WiFi設定の読み込み、WiFi接続、mDNSの開始、NTPによる時刻同期、メッセージ出力、エラー処理など、ネットワークとファイルシステムに関連する補助的な関数をまとめています。`main.cpp` の `setup()` 関数から呼び出される `setupNetwork()` が中心的な役割を果たします。
*   **主な内容:**
    *   **インクルード:** `SDU_webDav.h` をインクルードします。
    *   **関数プロトタイプ宣言:** このファイル内で定義されている関数の宣言です。
    *   **定数定義:** ファイルシステムの種類を示す定数 (`FS_SPIFFS`, `FS_SD`) や、NTPサーバーのアドレスなどを定義します。
    *   **グローバル変数:** WiFi設定 (`SSID`, `SSID_PASS`, `HOST_NAME`)、IPアドレス (`IP_ADDR`)、ファイルシステムの有効状態 (`SD_ENABLE`, `SPIFFS_ENABLE`) などを保持します。
    *   **`setupNetwork()` 関数:**
        *   プログラム名、バージョン、GitHub URLなどを表示します。
        *   `FS_start()` を呼び出してSDカードとSPIFFSを初期化しようと試みます。
        *   `getWiFiSettings()` を呼び出して、SDカードまたはSPIFFS上の `wifi.txt` ファイルからWiFi設定を読み込みます。ファイルがない場合は `main.cpp` で定義されたデフォルト値を使用します。
        *   `wifiStart()` を呼び出してWiFiネットワークに接続します。
        *   `mdnsStart()` を呼び出してmDNSサービスを開始し、ホスト名でアクセスできるようにします。
        *   `configTime()` でNTPサーバーに接続し、デバイスの時刻を合わせます。
        *   最終的なIPアドレスなどを表示します。
        *   途中でエラーが発生した場合は `false` を返します。
    *   **`prt()` 関数:** シリアルモニタとM5Stackのディスプレイの両方にメッセージを出力します (`DISP_ON` が `true` の場合)。
    *   **`wifiStart()` 関数:** WiFiへの接続処理を行います。接続試行を繰り返し、タイムアウトした場合は `false` を返します。
    *   **`mdnsStart()` 関数:** mDNSを開始し、指定されたホスト名をネットワークに登録します。
    *   **`getWiFiSettings()` 関数:** 指定されたファイルシステム (SD or SPIFFS) から `wifi.txt` を読み込み、内容を解析して `SSID`, `SSID_PASS`, `HOST_NAME` 変数に格納します。ファイルが存在しない、または内容が不正な場合は `false` を返します。
    *   **`STOP()` 関数:** 致命的なエラーが発生した場合に、ファイルシステムを停止し、エラーメッセージを表示して無限ループに入り、プログラムを停止させます。
    *   **`FS_start()` 関数:** 指定されたファイルシステム (SD or SPIFFS) を初期化します。SDカードの場合は `SD_cardInfo()` でカード情報もチェックします。
    *   **`SD_cardInfo()` 関数:** 接続されているSDカードの種類を検出し、情報をシリアルモニタに出力します。カードが認識できない場合は `false` を返します。

## まとめ

これらのファイルは連携して、M5Stackデバイス上で動作するWebDAVサーバーを実現しています。`main.cpp` が全体の流れを制御し、`fs_util.cpp` がネットワーク接続やファイルシステムの準備といった下準備を担当、`SDU_webDav.h` がそれらの間で共通の定義や宣言を提供し、`SDUpdater.cpp` がオプションでSDカードからのファームウェア更新機能を追加します。これにより、ユーザーはWiFi経由でM5Stackのファイルシステムに簡単にアクセスできるようになります。

