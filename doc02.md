# SDU-WebDav 取扱説明書

## 1. はじめに

SDU-WebDavは、M5StackなどのESP32デバイス上で動作するWebDAVサーバーです。
このプログラムを使用すると、WiFiネットワーク経由で、お使いのPCからデバイスのファイルシステム（SDカードまたは内蔵フラッシュメモリ(SPIFFS)）にアクセスし、ファイルの読み書きや管理を行うことができます。

**主な機能:**

*   WebDAVプロトコルによるファイルアクセス
*   SDカードまたはSPIFFSのファイルシステムに対応 (コンパイル時に選択)
*   WiFi経由でのリモートアクセス
*   mDNSによるホスト名でのアクセス (`http://<ホスト名>.local/DavWWWRoot`)
*   (オプション) SDカードからのファームウェア更新機能 (`M5StackUpdater` を利用)
*   NTPによる時刻同期 (内部的に実行)

**対象デバイス:**

*   M5Stackシリーズ (Core, Core2, CoreS3など、SDカードスロットのCSピンがGPIO 4であること)
*   その他のESP32開発ボード (設定変更が必要な場合があります)

**ソースコード:**

*   https://github.com/NoRi-230401/SDU-WebDav

## 2. 必要なもの

*   **ESP32デバイス:** M5Stackなど
*   **SDカード:** SDカード版を使用する場合 (FAT32推奨)
*   **WiFi環境:** 接続するWiFiネットワークのSSIDとパスワード
*   **PC:** WebDAVクライアントとして接続するためのコンピュータ (Windows, macOS, Linuxなど)
*   **USBケーブル:** ファームウェア書き込みやシリアルモニタ確認用

## 3. 準備

### 3.1. ファームウェアの書き込み

1.  **ソースコードの取得:** 上記GitHubリポジトリなどからソースコードを入手します。
2.  **コンパイル環境の準備:** Arduino IDEまたはPlatformIOなどの開発環境を準備します。
3.  **ライブラリのインストール:** 必要なライブラリをインストールします。
    *   `M5Unified`
    *   `ESPWebDAV`
    *   `M5StackUpdater` (`ENABLE_SD_UPDATER` を有効にする場合)
4.  **コンパイルオプションの設定 (platformio.ini等):**
    *   **ファイルシステム選択:**
        *   SDカードを使用する場合: `-DENABLE_SD_SELECT` を定義します。(プログラム名は `SDU-WebDav SD` になります)
        *   SPIFFSを使用する場合: 上記を定義しません。(プログラム名は `SDU-WebDav SPIFFS` になります)
    *   **SDアップデーター:**
        *   SDカードからのファームウェア更新機能を使用する場合: `-DENABLE_SD_UPDATER` を定義します。
5.  **コンパイルと書き込み:** ソースコードをコンパイルし、USBケーブルで接続したESP32デバイスに書き込みます。

### 3.2. WiFi設定ファイルの作成 (`wifi.txt`)

デバイスが接続するWiFiネットワークの情報を `wifi.txt` というファイルに記述します。このファイルは、使用するファイルシステム（SDカードまたはSPIFFS）の**ルートディレクトリ**に配置してください。

**ファイル形式:**



*   **1行目:** WiFiネットワークのSSID
*   **2行目:** WiFiネットワークのパスワード
*   **3行目:** デバイスのホスト名 (ネットワーク上で識別するための名前、例: `m5stack-webdav`)。mDNSで使用されます。

**注意点:**

*   各行の前後には余計なスペースを入れないでください。
*   文字コードはUTF-8 (BOMなし) を推奨します。
*   このファイルが存在しない場合、`main.cpp` 内に記述されたデフォルト設定 (`YOUR_SSID`, `YOUR_SSID_PASS`, `YOUR_HOST_NAME`) が使用されますが、通常は `wifi.txt` を作成することを強く推奨します。デフォルト値は以下の通りです。
    *   `YOUR_SSID`: "your_wifi_ssid"
    *   `YOUR_SSID_PASS`: "your_wifi_ssid_password"
    *   `YOUR_HOST_NAME`: "stackchan"

### 3.3. SDカードの準備 (SDカード版を使用する場合)

1.  **フォーマット:** SDカードをPCでFAT32形式でフォーマットします。
2.  **`wifi.txt` の配置:** 上記で作成した `wifi.txt` ファイルをSDカードのルートディレクトリにコピーします。
3.  **ファームウェア更新用ファイル (オプション):** `ENABLE_SD_UPDATER` を有効にした場合は、更新に使用する `.bin` ファイル (通常 `menu.bin` または `app.bin`) をSDカードのルートにコピーします。
4.  **デバイスへの挿入:** 準備したSDカードをESP32デバイスのSDカードスロットに挿入します。

## 4. 使い方

### 4.1. 起動

1.  準備が完了したら、ESP32デバイスの電源を入れます (USB給電またはバッテリー)。
2.  デバイスのディスプレイ (M5Stackの場合、`DISP_ON = true` の時) およびシリアルモニタ (PC接続時、ボーレート115200) に起動ログが表示されます。

### 4.2. 起動時の表示内容

起動プロセス中に以下の情報が表示されます。

1.  **プログラム名とバージョン:** `SDU-WebDav SD` または `SDU-WebDav SPIFFS` とバージョン番号 (例: `v103a-250425`)。
2.  **ファイルシステムの初期化:** SDカード (`SD ..... OK/NG`) や SPIFFS (`SPIFFS ..... OK/NG`) の初期化結果。
3.  **WiFi設定の読み込み:** `wifi.txt` から設定を読み込んだかどうかのメッセージ (`Settings read from SD/SPIFFS`)。
4.  **WiFi接続:** 指定されたSSIDへの接続試行と結果 (`WiFi ..... OK/NG`)。
5.  **mDNS開始:** ホスト名の登録結果 (`mDNS ..... OK/NG`)。
6.  **接続情報:** 成功すると、デバイスのIPアドレス (`IP Addr: xxx.xxx.xxx.xxx`) とWebDAV接続用パス (`\HOSTNAME\DavWWWRoot`) が表示されます。

### 4.3. PCからの接続方法

WebDAVサーバーが正常に起動したら、PCからファイルシステムにアクセスできます。

**Windows (エクスプローラー):**

1.  エクスプローラーを開きます。
2.  アドレスバーに以下のいずれかを入力し、Enterキーを押します。
    *   `\<ホスト名>\DavWWWRoot` (例: `\m5stack-webdav\DavWWWRoot`)
    *   `http://<IPアドレス>/DavWWWRoot` (例: `http://192.168.1.10/DavWWWRoot`)
    *   (接続できない場合) `http://<ホスト名>.local/DavWWWRoot` (例: `http://m5stack-webdav.local/DavWWWRoot`)
3.  ネットワーク資格情報の入力を求められる場合がありますが、通常は不要です。そのまま接続できるはずです。
4.  接続に成功すると、デバイスのファイルシステム（SDカードまたはSPIFFS）の内容がエクスプローラーに表示されます。

**macOS (Finder):**

1.  Finderを開きます。
2.  メニューバーから「移動」>「サーバへ接続...」を選択します。
3.  サーバアドレス欄に以下のいずれかを入力し、「接続」をクリックします。
    *   `http://<ホスト名>.local/DavWWWRoot` (例: `http://m5stack-webdav.local/DavWWWRoot`)
    *   `http://<IPアドレス>/DavWWWRoot` (例: `http://192.168.1.10/DavWWWRoot`)
4.  ユーザー名とパスワードの入力を求められる場合がありますが、通常は「ゲスト」として接続するか、何も入力せずに接続を試みてください。
5.  接続に成功すると、Finderのサイドバーやウィンドウにデバイスが表示され、ファイルシステムにアクセスできます。

**その他のクライアント:**

CarotDAV、Cyberduck、WinSCPなどのWebDAVクライアントソフトウェアを使用することも可能です。接続先URLとして上記のアドレスを指定してください。

### 4.4. ファイル操作

WebDAVで接続した後、PCのエクスプローラーやFinder上で、通常のフォルダと同じように以下の操作が可能です。

*   ファイルの閲覧、コピー、移動、削除
*   フォルダの作成、名前変更、削除
*   ファイルのアップロード（PCからデバイスへ）
*   ファイルのダウンロード（デバイスからPCへ）

## 5. SDカードからのファームウェア更新 (オプション)

`ENABLE_SD_UPDATER` オプションを有効にしてコンパイルした場合、起動時にSDカードからのファームウェア更新機能が利用できます。

1.  **起動時のメニュー:** デバイス起動直後、ディスプレイにプログラム名とメニューボタンが表示されます (`SDU_lobby`)。
2.  **更新ファイルの確認:** SDカードのルートに `menu.bin` (または設定されたファイル名) が存在するか確認されます。
3.  **更新の実行:**
    *   更新ファイルが見つかると、更新を促すメッセージが表示されます。
    *   M5Stackのボタン操作（通常は中央ボタン）で更新を開始できます。
    *   指定時間 (`SDU_SKIP_TMR` で設定、デフォルト5秒) 操作がない場合、または左ボタン (`< Menu` ボタン) を押すと、更新をスキップして通常のWebDAVサーバーが起動します。
4.  **更新プロセス:** 更新が開始されると、画面の指示に従ってください。完了するとデバイスは自動的に再起動します。

## 6. トラブルシューティング

*   **WiFiに接続できない:**
    *   `wifi.txt` のSSIDとパスワードが正しいか確認してください。
    *   WiFiルーターの電波強度を確認してください。
    *   デバイスをルーターの近くで試してみてください。
    *   シリアルモニタで詳細なエラーメッセージを確認してください (`cannot connect ,Wifi faile!` など)。
*   **WebDAVに接続できない:**
    *   デバイスに表示されたIPアドレスやホスト名が正しいか確認してください。
    *   PCとESP32デバイスが同じネットワークに接続されているか確認してください。
    *   PCのファイアウォール設定がWebDAV接続をブロックしていないか確認してください。
    *   接続アドレスの末尾に `/DavWWWRoot` を付けているか確認してください。
    *   mDNSでの接続がうまくいかない場合は、IPアドレスでの接続を試してください。
*   **SDカードが認識されない:**
    *   SDカードが正しく挿入されているか確認してください。
    *   SDカードがFAT32でフォーマットされているか確認してください。
    *   別のSDカードで試してみてください。
    *   シリアルモニタでSDカード関連のエラーメッセージを確認してください (`SD ..... NG`, `ERR: SD begin erro...`, `ERR: No SD card attached` など)。M5Stackの場合、CSピンはGPIO 4に接続されている必要があります。
*   **動作がおかしい、エラーが出る:**
    *   シリアルモニタを接続し、起動時や操作時のログを確認してください。エラーメッセージが問題解決の手がかりになります。
    *   `STOP()` 関数が呼ばれるような致命的なエラーの場合、シリアルモニタに `*** Stop *** fatal error` と表示され、処理が停止します。原因（ファイルシステムエラー、設定不備など）をログから特定してください。

## 7. その他

*   **ディスプレイ表示:** `main.cpp` 内の `DISP_ON` 変数を `false` に変更してコンパイルすると、M5Stackのディスプレイへのメッセージ表示を無効にできます。
*   **カスタマイズ:** ソースコード内のコメントや設定項目を参照して、必要に応じて動作をカスタマイズしてください（例: デフォルトWiFi設定、NTPサーバー (`fs_util.cpp` 内)、SDアップデーターのスキップ時間 (`SDUpdater.cpp` 内) など）。

---
この説明書がSDU-WebDavの利用に役立つことを願っています。

